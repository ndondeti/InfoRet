Both the searh mechanisms are inside the package "edu.asu.ir13"
The TF based search is done in the class "SearchFilesTF.java"
The IDF based search is done in the class "SearchFilesIDF.java"

Each of them can be run induvidually to perform the corresponding search.

The class DocumentSimilarity is used to rank the documents.